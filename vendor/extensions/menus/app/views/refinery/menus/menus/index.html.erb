<% content_for :body do %>
  <ul id="menus">
    <% @menus.each do |menu| %>
      <li>
        <%= link_to menu.name, refinery.menus_menu_path(menu) %>
        <% binding.pry %>
      </li>

    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
